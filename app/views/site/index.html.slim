
p.welcome Find food trucks with full of noms!

div class='form-group'
  =render 'search'

br

div class="form-group"
  = render 'mailchimp'

div class="row" id="hello"
  = image_tag "default_logolg.png", class: 'img-responsive', id: "main_logo"

/ = link_to(image_tag("default_logolg.png"), root_path, id: "main_logo")
/ click here and IF == loggedin => see Truckview/AddTruck page.
/ click here and IF !== loggedin => see Login/Signup page.
